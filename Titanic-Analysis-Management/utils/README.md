# 🧰 Utils

Titanic分析で共通的に利用する **関数・スクリプト・設定ファイル** を格納します。  
複数ノートブック間で重複しやすい処理をモジュール化し、再利用性を高めます。

---

## 📁 構成（推奨例）

| ファイル名 | 役割 |
|-------------|------|
| `preprocessing.py` | 欠損値補完・ラベルエンコード・正規化などの処理関数 |
| `feature_engineering.py` | 特徴量作成・派生特徴量生成 |
| `metrics.py` | 評価指標やスコア算出関数 |
| `visualization.py` | 可視化（学習曲線、重要度グラフなど） |
| `config.yaml` | パス・共通設定などを集中管理 |

---

## 🚀 利用例

```python
from utils.preprocessing import fill_missing_values
from utils.feature_engineering import add_family_size

train = fill_missing_values(train)
train = add_family_size(train)
